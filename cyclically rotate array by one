   method 1
   void rotate(int arr[], int n)
{
    if(n<=1)return;
    int temp=arr[n-1];
    int sub=arr[0];
    for(int i=1;i<n;i++)
     swap(arr[i],sub);

    arr[0]=temp;
    
}

method 2
void rotate(int arr[],int n){

for(int i=n-1;i>=1;i--)
swap(arr[i],arr[i-1]);

}
